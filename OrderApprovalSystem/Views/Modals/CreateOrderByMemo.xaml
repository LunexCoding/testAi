<UserControl 
    x:Class="OrderApprovalSystem.Views.Modals.CreateOrderByMemo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Служебная записка -->
        <Grid Grid.Row="0" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Номер служебной записки:" />
                <TextBox Text="{Binding MemoNumber, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Автор:" />
                <TextBox Text="{Binding MemoAuthor, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Заказ/Номер - два поля в строку -->
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Заказ:" />
                <TextBox Text="{Binding Order, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Номер:" />
                <TextBox Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Номенклатурная группа -->
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock Text="Номенклатурная группа:" />
            <ComboBox x:Name="NomenclatureGroupComboBox"
                      ItemsSource="{Binding NomenclatureGroups}"
                      SelectedItem="{Binding SelectedNomenclatureGroup, UpdateSourceTrigger=PropertyChanged}"
                      IsReadOnly="True"
                      Margin="0,5,0,0">
            </ComboBox>
        </StackPanel>

        <!-- Тип оснастки -->
        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="Тип оснастки:" />
            <ComboBox ItemsSource="{Binding EquipmentTypes}"
                      SelectedItem="{Binding SelectedEquipmentType, UpdateSourceTrigger=PropertyChanged}"  
                      Margin="0,5,0,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding TypeName}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <!-- Чертеж и Обозначение - два поля в строку -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                
                <TextBlock Text="Чертеж:" />
                <TextBox Text="{Binding Draft, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Обозначение:" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!--
        <StackPanel Grid.Row="5" Margin="0,10,0,0">
        <TextBlock Text="Комментарий:" />
        <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"
              Margin="0,5,0,0">
        </TextBox>
        </StackPanel>
         -->
    </Grid>
</UserControl>