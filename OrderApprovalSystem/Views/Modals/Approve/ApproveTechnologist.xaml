<UserControl 
    x:Class="OrderApprovalSystem.Views.Modals.ApproveTechnologist"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Дата выполнения ОТОИП - заблокированное поле ввода -->
        <StackPanel Grid.Row="0" Margin="0">
            <TextBlock Text="Срок выполнения:" />
            <DatePicker SelectedDate="{Binding ManufacturingTerm, UpdateSourceTrigger=PropertyChanged}"
               DisplayDateStart="{x:Static sys:DateTime.Today}"
               DisplayDate="{x:Static sys:DateTime.Today}"
               FirstDayOfWeek="Monday"
               IsTodayHighlighted="True"
               SelectedDateFormat="Short"
               Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBlock Text="Комментарий:" />
            <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                TextWrapping="Wrap"
                AcceptsReturn="True" 
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled"/>
        </StackPanel>
    </Grid>
</UserControl>